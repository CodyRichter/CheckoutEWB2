package main.test;

import org.junit.Test;

import static org.junit.Assert.assertTrue;

/**
 * CheckoutEWB2: Testing of Large-Scale data additions/manipulations/removals and data saving.
 * This will be run to ensure that the program is robust enough to handle data flow at the auction
 * event, as a failure at that time would be critical.
 *
 * @author Cody R.
 * @version 1.0
 */
public class LargeScaleTest {

    @Test
    public void testTest() {
        assertTrue(true);

    }

}
